# FPO Economics Management System Environment Configuration

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fpo_economics
DB_USER=postgres
DB_PASSWORD=your_password_here

# Alternative: Use DATABASE_URL (overrides individual DB_* variables)
# DATABASE_URL=postgresql://username:password@host:port/database

# Database Connection Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_ECHO=false

# Application Settings
LOG_LEVEL=INFO
DEBUG=false

# Security Settings
SECRET_KEY=your_secret_key_here

# External Service Configuration (for MCP integration)
# Add your external service configurations here